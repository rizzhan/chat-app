info:
  name: Send message
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:5000/api/messages
  body:
    type: json
    data: |-
      {
        "conversationId": "6a76ada98199b7c3914b5adb",
        "text": "Hello Alex! This is a test message."
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
