info:
  name: LOGIN (receiver)
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  body:
    type: json
    data: |-
      {
        "username": "Alex",
        "email": "Alex@example.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
