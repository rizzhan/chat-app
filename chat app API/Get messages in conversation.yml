info:
  name: Get messages in conversation
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:5000/api/messages/{{conversationId}}
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
