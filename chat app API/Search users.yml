info:
  name: Search users
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:5000/api/users/search?username=a
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
